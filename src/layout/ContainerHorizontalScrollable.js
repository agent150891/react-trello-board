import styled from 'styled-components';
import FullWidthContainer from './FullWidthContainer';

const ContainerHorizontalScrollable = styled(FullWidthContainer)`
    max-width:100%;
    overflow-x: auto;
`

export default ContainerHorizontalScrollable;